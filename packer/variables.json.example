{
    "project_id": "The project ID that will be used to launch instances and store images.",
    "machine_type": "The machine type. Defaults to "n1-standard-1",
    "disk_type": "Type of disk used to back your instance, like pd-ssd or pd-standard. Defaults to pd-standard.",
    "disk_size": "10",
    "preemptible": "false",
    "tags": "Assign network tags to apply firewall rules to VM instance",
    "netwoork": "The Google Compute network id or URL to use for the launched instance. Defaults to default. If the value is not a URL, it will be interpolated to projects/((network_project_id))/global/networks/((network)).",
    "subnetwork": "The Google Compute subnetwork id or URL to use for the launched instance. Only required if the network has been created with custom subnetting. Note, the region of the subnetwork must match the region or zone in which the VM is launched. If the value is not a URL, it will be interpolated to projects/((network_project_id))/regions/((region))/subnetworks/((subnetwork))"
}
